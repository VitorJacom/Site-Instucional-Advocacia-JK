---
import { getCollection } from "astro:content";

interface Props {
  limitar?: boolean;
}
const { limitar = false } = Astro.props;

const postsNoticias = await getCollection("noticias");

// Ordenar as notícias pela data (mais recente primeiro)
const postsOrdenados = postsNoticias.sort((a, b) => b.data.data.getTime() - a.data.data.getTime());
const totalNoticias = postsOrdenados.length;
const postsFiltrados = limitar ? postsOrdenados.slice(0, 4) : postsOrdenados;

const noticias = postsFiltrados.map((noticia) => {
  // Formata a data para "15 Out 2023"
  const dataFormatada = new Intl.DateTimeFormat("pt-BR", {
    day: "2-digit",
    month: "short",
    year: "numeric",
  })
    .format(noticia.data.data)
    .replace(".", ""); // Remove o ponto do mês se houver

  return {
    data: dataFormatada,
    titulo: noticia.data.titulo,
    resumo: noticia.data.resumo,
    link: noticia.data.link_externo || `/noticias/${noticia.id}/`,
    isExterno: !!noticia.data.link_externo,
  };
});
---

<section class="noticias-section">
  <div class="container">
    <h2 class="section-title">Últimas Notícias</h2>
    <div class="noticias-list">
      {
        noticias.length > 0 ? (
          noticias.map((noticia) => (
            <div class="noticia-item">
              <h3 class="noticia-title">
                <span>{noticia.data}</span> - {noticia.titulo}
              </h3>
              <p class="noticia-resumo">{noticia.resumo}</p>
              <a 
                href={noticia.link} 
                class="noticia-link"
                target={noticia.isExterno ? "_blank" : undefined}
                rel={noticia.isExterno ? "noopener noreferrer" : undefined}
              >
                Ver Notícia
                {noticia.isExterno && (
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                  </svg>
                )}
              </a>
            </div>
          ))
        ) : (
          <p>Nenhuma notícia publicada.</p>
        )
      }
    </div>

    {limitar && totalNoticias > 6 && (
      <div class="ver-todos-container">
        <a href="/noticias/todas/" class="btn-ver-todos">
          Ver Todas as Notícias
        </a>
      </div>
    )}
  </div>
</section>

<style>
  .noticias-section {
    padding: 5rem 2rem;
    background-color: white;
  }
  .container {
    max-width: 900px;
    margin: 0 auto;
  }
  .section-title {
    font-family: "Playfair Display", serif;
    font-size: 2.5rem;
    color: #1e293b;
    text-align: center;
    margin-bottom: 3rem;
  }
  .noticias-list {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }
  .noticia-item {
    border-bottom: 1px solid #e2e8f0;
    padding-bottom: 2rem;
  }
  .noticia-item:last-child {
    border-bottom: none;
  }
  .noticia-title {
    font-family: "Playfair Display", serif;
    font-size: 1.4rem;
    color: #1e293b;
    margin-bottom: 0.5rem;
  }
  .noticia-title span {
    font-family: sans-serif;
    font-size: 1rem;
    color: #64748b;
    font-weight: normal;
  }
  .noticia-resumo {
    font-family: sans-serif;
    font-size: 1rem;
    color: #475569;
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  .noticia-link {
    font-family: sans-serif;
    font-size: 0.9rem;
    font-weight: bold;
    color: #519cae;
    text-decoration: underline;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
  }
  .noticia-link svg {
    width: 0.9rem;
    height: 0.9rem;
    flex-shrink: 0;
  }
  
  .ver-todos-container {
    display: flex;
    justify-content: center;
    margin-top: 3.5rem;
  }
  .btn-ver-todos {
    font-family: sans-serif;
    font-size: 0.9rem;
    font-weight: bold;
    color: #519cae;
    border: 2px solid #519cae;
    background-color: transparent;
    padding: 0.85rem 2.5rem;
    text-align: center;
    text-decoration: none;
    border-radius: 4px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all 0.3s ease;
  }
  .btn-ver-todos:hover {
    background-color: #519cae;
    color: white;
    transform: translateY(-2px);
  }
</style>
